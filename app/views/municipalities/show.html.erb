<div class="flex flex-col">
  <h1 class="font-bold"><%= @municipality.name %></h1>
  <% @municipality.parishes.each do |parish| %>
    <div class="flex">
      <%= parish.name %>
    </div>
  <% end %>
</div>
