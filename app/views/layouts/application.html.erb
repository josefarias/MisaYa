<!DOCTYPE html>
<html>
  <head>
    <title>MisaYa</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= tag.style(".translation_missing { outline: 1px solid red; }") if Rails.env.development? %>
  </head>

  <body>
    <main class="mt-28 container flex px-5 mx-auto mb-10">
      <%= yield %>
    </main>
  </body>
</html>
